@model Teller.Web.ViewModels.Search.SearchUserViewModel

<div class="text-center">
    @*<div class="col-md-2">
            <img src="@user.AvatarPath" alt="@user.Username" />
        </div>*@
    <h4>
        @Html.ActionLink(Model.Username, "Index", "UserInfo", new { Area = "User", id = Model.Username }, new { })
    </h4>
</div>
