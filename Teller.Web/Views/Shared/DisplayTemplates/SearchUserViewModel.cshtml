@model Teller.Web.ViewModels.Search.SearchUserViewModel

<div class="text-center">
    @*<div class="col-md-2">
            <img src="@user.AvatarPath" alt="@user.Username" />
        </div>*@
    <h4>
        @Html.ActionLink(Model.Username, "Index", "Info", new { Area = "User", username = Model.Username }, new { })
    </h4>
</div>
