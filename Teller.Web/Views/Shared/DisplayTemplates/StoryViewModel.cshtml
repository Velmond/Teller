@model Teller.Web.ViewModels.Story.StoryViewModel
@using Teller.Web.Infrastructure
@{
    var urlGenerator = new UrlGenerator();
}

<div class="row">
    <div class="col-sm-2">
        <img src="@Model.PicturePath" alt="@Model.Title" class="img-responsive img-circle img-thumbnail"  />
    </div>
    <div class="col-sm-10">
        <h4>
            @Html.ActionLink(Model.Title, "Index", "Story", new { Area = "", id = urlGenerator.GenerateUrlId(Model.Id, Model.Title) }, new { })
        </h4>
        <p>@Html.Raw(Model.Content)</p>
        <p class="pull-right">
            <small>@Html.DisplayFor(m => Model.DatePublished) | @Html.ActionLink(Model.Author, "Index", "Story", new { Area = "User", id = Model.Author }, new { })</small>
        </p>
    </div>
</div>

