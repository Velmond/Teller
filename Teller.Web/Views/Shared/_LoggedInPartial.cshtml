<ul class="nav navbar-nav collapse navbar-collapse pull-right">
    <li>@Html.RouteLink("Tell Story", new { Area = string.Empty, Controller = "Story", Action = "Create" }, null)</li>
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
            Stories<b class="caret"></b>
        </a>
        <ul class="dropdown-menu">
            <li>
                @Html.RouteLink("Subscriptions", new { Area = string.Empty, Controller = "Feed", Action = "Index" }, new { })
            </li>
            <li>
                @Html.RouteLink("Saved for later", new { Area = string.Empty, Controller = "Feed", Action = "ReadLater" }, new { })
            </li>
            <li>
                @Html.RouteLink("Favorites", new { Area = string.Empty, Controller = "Feed", Action = "Favorites" }, new { })
            </li>
        </ul>
    </li>
</ul>
