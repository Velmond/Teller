@model IEnumerable<Teller.Web.ViewModels.Story.UserFeedStory>
@{
    ViewBag.Title = "Read later";
    var pager = new Teller.Web.ViewModels.Pager.FeedPagerViewModel()
    {
        Area = "",
        Action = "ReadLater",
        Controller = "Feed",
        PagesCount = (int)ViewBag.Pages,
        Page = (int)ViewBag.Page,
        Id = ViewBag.Id
    };
}

<div class="row">
    <div class="col-sm-offset-1 col-sm-10">
        <div class="well-sm">
            <div class="row">
                <h3 class="lead">
                    <span class="pull-left">
                        Saved for later
                    </span>
                    @if(ViewBag.Pages > 1)
                    {
                        @Html.Partial("_Pager", pager)
                    }
                </h3>
            </div>
        </div>

        <div class="row">
            <div class="well">
                @Html.Partial("_FeedStories", Model)
            </div>
        </div>        @if(ViewBag.Pages > 1)
        {
            <div class="well-sm">
                <div class="row">
                    <h3 class="lead">
                        @Html.Partial("_Pager", pager)
                    </h3>
                </div>
            </div>
        }
    </div>
</div>

